/**
 * Perceptual space dimension — must match VECTOR_DIMENSION on the Scala backend
 * (Reality Engine, Perception Engine, VectorStore / Qdrant collection).
 *
 * Change only here; all components import PERCEPTUAL_DIM from this file.
 */
export const PERCEPTUAL_DIM = 768;
